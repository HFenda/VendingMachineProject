[tool.poetry]
name = "VendingMachineProject"
version = "0.1.0"
description = "FastAPI Vending Machine API"
authors = ["HFenda <harun.aliefendic.ha@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.9"
fastapi = "^0.111.0"
uvicorn = "^0.30.1"
pydantic = "^1.9.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"